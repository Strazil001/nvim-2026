return {
  "folke/snacks.nvim",
  opts = {
    picker = {
      layout = {
        layout = {
          backdrop = false, -- Removes the dark dim layer behind the floating window
        },
      },
    },
  },
}
